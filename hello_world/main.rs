fn main() { // main is the first code that runs 
    println!("Hello, world!"); // using ! means we're using a Rust macro
}

// Now you can run this using "rustc main.rs" (which compiles it), ./main, which runs the executable

// Cargo is build system and package manager
